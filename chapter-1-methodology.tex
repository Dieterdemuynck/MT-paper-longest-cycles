\chapter{Methodology}
\label{cha:methodology}
\todo{methodology introduction. To write in this chapter: literature study (including known structures), algorithms used, tests that have been written, language used and why, Makefile maybe?}
% A chapter is a logical unit. It normally starts with an introduction, which you are reading now. The last topic of the chapter holds the conclusion.
In this chapter, we explain in detail what graph generators we used, the algorithms written for the research, the relevant graphs from previous research, the programming language the algorithms were written in and why, and the tests performed to make sure the programs are correctly written.

\section{General Structure}

\todo{Pipelining, generators to algorithm (with language choice) to output}

Since the goal of this thesis is to exhaustively check a large number of graphs, an efficient implementation is required. We will use multiple programs and processes to solve this problem (see section~\ref{sec:generators} on graph generators), and therefore chose to use Linux' pipelining capabilities to efficiently pass output from one program as input to another. The implementation of the developed algorithms must also be efficient, and for that reason, \texttt{C++} was chosen as the programming language to implement all algorithms discussed in section~\ref{sec:algorithms}. To make analysis of the results easier, the output for both programs is put in a CSV format, with header.

\section{Graph Generators}
\label{sec:generators}

\todo{what is a graph generator?}
\todo{More examples of generators, and their intended graph classes (algemene grafen geng (nauty), kubische grafen snarkhunter, planaire grafen plantri)}

Efficient but exhaustive graph generation is a difficult problem to solve. A graph generator needs to be exhaustive (i.e. generate every graph in a certain category), while also avoiding generating isomorphic graphs. If not much care is put into generating each graph once, the same graph can easily be generated multiple times as isomorphic graphs, resulting in redundant computations. Many graph generators have already been made, each of which already took a lot of research~\cite{article:jorik-jooken}. That is why, rather than making an attempt at our own graph generator specific for this problem, we use existing graph generators in this thesis.

\subsection{Nauty and GenerateUHG}

The \texttt{Nauty} package~\cite{lib:nauty} has a graph generator \texttt{geng} which can ``generate non-ismorphic graphs very quickly''~\cite{lib:nauty}, including specifically 2-connected graphs. It can also output its graphs in the \texttt{graph6} format, which is a compact ASCII string representation of graphs. This creates a useful graph generator for RQ\ref{rq1}.

\todo{Add: Why 2-connected? (Well, it's the question we're trying to answer, no?)}

During the year we suspected that most of the generated 2-connected graphs are hamiltonian. Since a hamiltonian graph has a cycle that goes through all vertices of the graph, this would mean that the graphs of order 13 and higher would not have circumference 11. We would thus like to avoid hamiltonian graphs completely. This is why, for RQ\ref{rq1}, we instead opted to use the Uniquely Hamiltonian Graphs generator~\cite{lib:UHG}, which holds a program \texttt{GenerateUHG} that allows us to generate graphs with a specific amount of hamiltonian cycles, in the \texttt{graph6} format. By setting that amount to 0, we only get non-hamiltonian graphs, which we suspect to have a lower quantity of graphs than 2-connected graphs. For larger $n$, these suspicions seem to be confirmed, as seen in Table~\ref{tab:2con-nonhamil}.

\begin{table}
  \centering
  \caption{Amount of 2-connected graphs~\cite{data:integer-sequence} and of nonhamiltonian graphs~\cite{lib:UHG} per order of the graph $n$}
  \label{tab:2con-nonhamil}
  \begin{tabular}{rrr}
    \hline\\
    $n$ & Number of 2-connected graph & Number of nonhamiltonian graphs \\
    \hline \\
    1 & 0 & 1 \\
    2 & 1 & 2 \\
    3 & 1 & 3 \\
    4 & 3 & 8 \\
    5 & 10 & 26 \\
    6 & 56 & 108 \\
    7 & 468 & 661 \\
    8	& 7123 & 6150 \\
    9	& 194066 & 97585 \\
    10 & 9743542 & 270050 \\
    11 & 900969091 & 135841840 \\
    12 & 153620333545 & 12568984762 \\
    13 & 48432939150704 & 2179513027405 \\
    \hline
  \end{tabular}
\end{table}

Using this generator instead reintroduced the problem of making sure the graphs are 2-connected, since the generator has no option to only generate 2-connected graphs. The \texttt{Nauty} package fortunately also contains the program \texttt{pickg},\todo{is this more formal?} which lets us filter the output of another program. It takes \texttt{graph6} strings as input, and only outputs those strings for which the graph is 2-connected, given the correct parameters \todo{include parameters}. This program is very efficient compared to generation and the rest of the algorithm\todo{proof}, and therefore the use of \texttt{GenerateUHG} with \texttt{pickg} seems to be a good choice for the graph generation of RQ\ref{rq1}.

\subsection{plantri}

In the second research question RQ\ref{rq2}, only planar graphs are relevant, and as such, a planar graph generator seems to be the most interesting. The \texttt{plantri} package~\cite{lib:plantri} includes a suitable generator \texttt{plantri} for this. With the correct parameters, this package can generate 2-connected planar graphs with minimum degree 4. This generator only generates non-isomorphic 2-connected graphs where isomorphism is defined with reference to the embedding on a sphere, ``which means that outputs may be isomorphic as abstract graphs if the connectivity is less than 3''~\cite{lib:plantri}, i.e. there may be some duplicate 2-connected graphs. Despite this redundancy, generating planar graphs remains more efficient than generating 2-connected graphs and checking their planarity. 

It has also been shown that all 3-connected planar graphs, with minimum degree 4, on at most 22 vertices are hamiltonian~\cite{article:hamiltonian-planar}. This means that up to 22 vertices, we simply have to generate graphs with connectivity 2, i.e. graphs which are 2-connected but not 3-connected. This reduces the search space even more, as seen in Table~\ref{tab:2con-planar}.

The parameters used include the option \texttt{-p} to select general plane simple graphs, the option \texttt{-m4} to select graphs with minimum degree 4, the option \texttt{-c2x} to select exclusively 2-connected graphs and not 3-connected graphs. Lastly, the option \texttt{-g} is used which forces the outputted graphs to be in the \texttt{graph6} format.

\begin{table}
  \centering
  \caption{Amount of generated 2-connected planar graphs with (parameters \texttt{pgm4c2}), and generated planar graphs with connectivity 2 (with parameters \texttt{pgm4c2x}) per order of the graph $n$}
  \label{tab:2con-planar}
  \begin{tabular}{rrr}
    \hline\\
    $n$ & 2-connected planar graphs & connectivity 2 planar graphs \\
    \hline \\
    6 & 1 & 0 \\
    7 & 1 & 0 \\
    8	& 4 & 0 \\
    9	& 14 & 0 \\
    10 & 69 & 2 \\
    11 & 445 & 17 \\
    12 & 3754 & 239 \\
    13 & 34563 & 2800 \\
    14 & 341758 & 34215 \\
    15 & 3470706 & 406005 \\
    16 & 35958178 & 4759110 \\
    17 & 377489455 & 55224800 \\
    \hline
  \end{tabular}
\end{table}


\subsection{Existing lists of graphs}

During development on the second research question, we quickly noticed hamiltonian graphs are irrelevant. This is because, regardless of if a graph has a cycle of length $k$ in each of their vertex-deleted subgraphs, if the graph has a hamiltonian cycle, the statement of RQ\ref{rq2} is trivially true. A supervisor of this thesis, Jan Goedgebeur, had previously done research on nonhamiltonian 2-connected planar graphs with minimum degree 4, and had saved these graphs of up to 20 vertices~\cite{article:hamiltonian-planar}. By using this list, we could push the total amount of relevant graphs further than with only generation.

%\subsection{An item}
%Please don't abuse enumerations: short enumerations shouldn't use
%``\verb|itemize|'' or ``\texttt{enumerate}'' environments.
%So \emph{never write}: 
%\begin{quote}
%  The Eiffel tower has three floors:
%  \begin{itemize}
%  \item the first one;
%  \item the second one;
%  \item the third one.
%  \end{itemize}
%\end{quote}
%But write:
%\begin{quote}
%  The Eiffel tower has three floors: the first one, the second one, and the
%  third one.
%\end{quote}

\section{Finding the Intersection of longest k-cycles}
\label{sec:algorithms}

\todo{Correctness tests}

\subsection{Intersection of cycles}

One of the aims of this thesis is to create an algorithm that can determine if a graph has circumference 11, and has no vertex meeting every 11-cycle for RQ\ref{rq1}. Finding the longest cycle in a general graph, known as longest cycle problem, ``is well known as NP-complete.''~\cite{article:longest-cycle-np}, meaning there is no known polynomial algorithm for solving it and remains computationally expensive. In this thesis, the approach used is to enumerate all cycles, which lets us determine if a cycle of length 11 exists and no larger. This enumeration also provides an easy method for computing the required intersection, as we just take the intersection of all found 11-cycles. Thus, we need an algorithm for enumerating all cycles efficiently.

\subsubsection{Enumeration of all cycles once}

The simplest approach to this problem is a depth-first search on the vertices. The algorithm goes as follows: take a starting vertex and add it to the current path. Then, repeat the following steps until the path is empty. Take an unvisited neighbor of the last vertex in the path, and consider that vertex as a candidate next vertex. If this candidate is the original starting vertex (and there are at least three vertices in the path), then we have found a cycle, and the vertices of the cycle are the visited vertices together with the starting vertex. The length of this cycle equal to the amount of vertices in the path. If the last vertex has no more unvisited edges, we can consider this vertex spent and pop it from the list of visited vertices, which also requires us to set its edges as unvisited again.

This algorithm by itself is not yet efficient, as during enumeration, each cycle is found once for every vertex it contains, times two. This because the algorithm takes every vertex of the cycle as a starting vertex, and discovers the cycle each time twice, in the two directions it can travel down the cycle. We can reduce this number by using a useful property of the way vertices of graphs are often represented in computer programs. In this thesis' source code, vertices are represented as integers, from 0 to $n-1$ with $n$ equal to the order of the graph. This provides us with the useful property that the vertices are \textit{ordered}.

The ordering of the vertices allows us to consider the starting vertex as the \textit{minimal} vertex, where we only allow new vertices to be candidates if they are higher in order than this starting vertex. The algorithm would then run starting from every This greatly reduces the number of times each cycle is enumerated, from twice for every vertex in both directions, to only twice from the minimal vertex of the cycle. Furthermore, we can also use the ordering of the vertices to force the cycle to only be enumerated in one of the two directions. This by requiring that the cycle's first and last vertex (other than the starting vertex) must be in, for example, ascending order, and ignoring found cycles which are in descending order. 

\subsubsection{Halting cycle enumeration early}

This algorithm allows us to enumerate all cycles of the graph exactly once, under the assumption the graph is connected. We can get an exact value of the circumference of the graph by setting the circumference equal to 0 at the start of the enumeration and, for each found cycle, taking the maximum of the current believed circumference and the length of the cycle. Note that for RQ\ref{rq1} we are only interested in the graphs with circumference 11. As a result, we can halt enumeration of the cycles early when a graph is found to have a cycle that is larger than 11, as said graph is no longer relevant for RQ\ref{rq1}.

Additionally, cycles that are smaller than 11 are also uninteresting. If no 11-cycle or larger is found, then the circumference of the graph is lower than 11, and again not relevant for RQ\ref{rq1}. Therefore, we can safely reduce the number of starting vertices, by skipping the 10 highest ordered nodes, as running DFS with these as minimal vertices can only result in cycles of length 10 or smaller.

During enumeration, we must also compute the intersection of all 11-cycles. Set the initial intersection $I$ equal to all vertices of the graph $V(G)$ at the start of enumeration. Whenever an 11-cycle is enumerated, set $I$ equal to the intersection of $I$ and the vertices of the 11-cycle. The result at the end of the enumeration will be the intersection of all 11-cycles if there are any, or all vertices otherwise. If there are no 11-cycles, then the computed circumference is smaller than 11 (or larger if computation halted early due to a larger cycle), and therefore irrelevant for RQ\ref{rq1}.

\todo{add explanation for why no direction in code, idea of comparison taking just as long or longer than the instructions}

\todo{add chosen data structures}

\begin{algorithm}
\caption{\textsc{FindIntersectionK}$(G,k,\prec)$}
\label{alg:find-k-intersection}

\begin{algorithmic}[1]
\Require Graph $G$, integer $|V(G)|>k>2$, and a total ordering on $V$
\Ensure intersection of all $k$-cycles $\textit{intersection}$ and circumference $c(G)=k$

\State $\textit{intersection} \gets V(G)$
\ForAll{$s \in V(G)$ ascending}
\State $\textit{path} \gets [s]$
\While{$\textit{path}$ is not empty}
    \State $u \gets$ the last vertex in $\textit{path}$
    \If{no unvisited neighbor of $u$}
        \State remove $u$ from $\textit{path}$
        \Comment Including its visited neighbors
        \State \textbf{continue}
    \EndIf
    \State {$v \gets$ an unvisited vertex, and unvisited neighbor of $u$}
    \State mark $v$ as visited neighbor of $u$
    \If{$v=s$}
        \State $\ell \gets |\textit{path}|$
        \If{$\ell > k$}
            \State \Return $\text{LARGER\_THAN\_K}$
        \EndIf
        \If{$\ell = k$}
            \State $\textit{intersection} \gets
            \textit{intersection} \cap \textit{path}$
        \EndIf
        \State \textbf{continue}
    \EndIf
    \If{$s \prec v$}
        \State add $v$ to $\textit{path}$
    \EndIf
\EndWhile
\EndFor

\If{$\textit{intersection} = V(G)$}
    \State \Return $\text{SMALLER\_THAN\_K}$
\EndIf
\State \Return $\text{EQUAL\_TO\_K}$
\end{algorithmic}
\end{algorithm}

\section{Checking vertex-deleted subgraphs for k-cycles}

\todo{Add RQ2 again for clarity}

This section introduces the algorithm for RQ\ref{rq2}. The approach is similar to RQ\ref{rq1}: we generate planar graphs with minimum degree 4, and check for each graph if in every vertex-deleted subgraph there exists a $k$-cycle, that there then is a cycle larger than $k$. Trivially, if a vertex-deleted subgraph of a graph $G$ has a $k$-cycle, then the graph $G$ itself also has a $k$-cycle. As a result, for a graph $G$ can only be a negative example of RQ\ref{rq2}, there must be a $k$-cycle in every vertex-deleted subgraph of $G$ and $G$ must also have circumference $k$. From this, we decided to focus our algorithm on finding negative examples, as we can assume all other graphs to be positive examples.

\todo{Something is missing here, in terms of circumference}
\begin{theorem}
  \label{theorem:intersection-vertex-deletion}
  A graph $G$ has a $k$-cycle in every vertex-deleted subgraph, and no cycle larger than $k$ in $g$ if and only if the intersection of the vertices of all $k$-cycles in the graph is empty, and $G$ has circumference $k$.
\end{theorem}

\begin{proof}
  Assume $G$ has a $k$-cycle in every vertex-deleted subgraph, and no cycle larger than $k$ exists in $G$. $G$ must have at least one $k$-cycle, denoted $C$, and therefore must have circumference $k$. The intersection of all $k$-cycles must be at most the vertices $V(C) \subset V(G)$. Per the assumption, for every vertex $v_i \in V(G)$ there must be a $k$-cycle $C_i'$ in $G$ avoiding the vertex $v_i$, including those vertices $v_i \in V(C)$. As a result, the intersection of all $k$-cycles is at most
  $$
  \bigcap_{i} C_i' \cap C = \emptyset
  $$
  and thus the intersection of all $k$-cycles must be empty.

  Now assume $G$ has circumference $k$ and the intersection of all $k$-cycles is empty. $G$ cannot be hamiltonian, as otherwise the intersection of the vertices of all $k$-cycles must be the vertices $V(G)$. $G$ must have at least one $k$-cycle $C$, which avoids all vertices $v_{\text{out}} \in V(G) \setminus V(C)$. Additionally, since the intersection of all $k$-cycles is empty, there must be a $k$-cycle for every vertex $v_{\text{in}} \in V(C)$ which avoids $v_{\text{in}}$. As a result, there exists a $k$-cycle avoiding every vertex $v \in V(C) \cup (V(G) \setminus V(C)) = V(G)$, and therefore every vertex-deleted subgraph of $G$ has a $k$-cycle.

  We conclude that for a graph $G$, $G$ has a $k$-cycle in every vertex-deleted subgraph and has circumference $k$ if and only if the intersection of the vertices of all $k$-cycles in the graph is empty.
\end{proof}


This result allows us to reuse most of Algorithm~\ref{alg:find-k-intersection}. To adapt it to RQ\ref{rq2}, instead of keeping track of the intersection of all $k$-cycles for an arbitrary value $k$, we must keep track of the intersection of the longest currently found cycles of the graph. This means that during execution, if a longer cycle is found than the current length of the longest cycles found, instead of halting early, we simply set the intersection of the longest cycles to the vertices of that new cycle.

Thomassen has shown~\cite{article:thomassen} that for every planar graph with minimum degree 4 of order $n$, if the graph has an $(n-1)$-cycle in every vertex-deleted subgraph, it must also be hamiltonian. This mean we can still halt the algorithm early, namely when a cycle is found of length $n-1$ or $n$.

\section{Study of Known Graphs}

The paper \textit{On a question of van Aardt et al. on destroying all longest cycles} by C. T. Zamfirescu~\cite{article:zamfirescu} constructs a family of graphs providing an answer for the values $k = 10, 13, 16$ to the open question posed by van Aardt et al. similar to RQ\ref{rq1}. We suspect that, if such a graph exists for $k=11$, then that graph may look similar to a graph from that family. Additionaly, some graphs in that family are also negative examples to RQ\ref{rq2}\todo{what values}. In this section, we will show this construction, how it has influenced our search space when running the programs, and how we intend to use this construction to potentially manually find interesting values for one of the two Research Questions.

\subsection{Construction by C. T. Zamfirescu}
\label{sec:tube-graph}

C. T. Zamfirescu showed~\cite{article:zamfirescu} that for every pair of integers $\ell \ge 2$ and $p \ge 4$, there exists a 2-connected graph G of order $()\ell+1) p$ and circumference $(\ell+1) (p-1) + 1$ whose intersection of all longest cycles is empty. We repeat the construction of the graph $G$ used in the proof here. Note that in this thesis, we use a different convention for $\ell$ than in the paper by C. Zamfirescu.
Take two cycles $(a_0, a_1, ..., a_{p-1})$ and $(b_0, b_1, ..., b_{p-1})$, each of length $p$, and connect $(a_i, b_i)$ for all $i$. Then, insert vertices on each edge $(a_i, b_i)$ until each becomes a path of length $\ell$ (i.e., has $\ell$ edges or $\ell+1$ vertices, including the vertices $a_i$ and $b_i$). Denote these paths from $a_i$ to $b_i$ as $H_i$. If $p$ is even, remove the edges $a_{p-1}a_0$ and $b_{p-1}b_0$, and replace them with the edges $a_{p-1}b_0$ and $b_{p-1}a_0$. We denote the graph constructed in this way as $G_{p,\ell}$. C. Zamfirescu has shown~\cite{article:zamfirescu} that the cycle $C$ with vertices

$$
\bigcup_{i \neq 0} V(H_i) \cup {b_0}
$$

and edges

$$
\bigcup_{i \neq 0} E(H_i)
  \cup {(a_j,a_{j+1})}_{j \text{odd}}
  \cup {(b_k,b_{k+1})}_{k>0 \text{even}}
  \cup \begin{cases}
    \emptyset\ \text{if}\ p \text{odd} \\
    (a_{p-1},b_0)\ \text{otherwise}
  \end{cases}
$$

is a longest cycle of the graph $G$ avoiding all vertices $v \in V(H_0) \setminus \{b_0\}$, and that a similar cycle can be constructed for all other vertices. Figure~\ref{fig:tube-graph-odd} shows the graph $G_{5,2}$ alongside one longest cycle in the graph, and similarly Figure~\ref{fig:tube-graph-even} shows the graph $G_{4,3}$ alongside one longest cycle.

\begin{figure}
  \centering
  \begin{subfigure}{0.4\textwidth}
    \centering
    \begin{tikzpicture}
      \TubeGraph{5}{2}
    \end{tikzpicture}
  \end{subfigure}
  \begin{subfigure}{0.4\textwidth}
    \centering
    \begin{tikzpicture}
      \TubeGraph{5}{2}
      \draw[red, very thick]
        (A1) --
        (A2) --
        (H2-1) --
        (B2) --
        (B3) --
        (H3-1) --
        (A3) --
        (A4) --
        (H4-1) --
        (B4) --
        (B5) --
        (H5-1) --
        (A5) --
        (A1);

      \foreach \v in {A1,A2,H2-1,B2,B3,H3-1,A3,A4,H4-1,B4,B5,H5-1,A5}
        \node[vertex, draw=red] at (\v) {};
    \end{tikzpicture}
  \end{subfigure}
  \caption{The graph construction $G_{p,\ell}$ by C. T. Zamfirescu~\cite{article:zamfirescu} for $p=5$ and $\ell=2$, alongside the same graph but with one longest cycle marked in red}
  \label{fig:tube-graph-odd}
\end{figure}

\begin{figure}
  \centering
  \begin{subfigure}{0.4\textwidth}
    \centering
    \begin{tikzpicture}
      \TubeGraph{4}{3}
    \end{tikzpicture}
  \end{subfigure}
  \begin{subfigure}{0.4\textwidth}
    \centering
    \begin{tikzpicture}
      \TubeGraph{4}{3}
      \draw[red, very thick]
        (A1) --
        (A2) --
        (H2-1) --
        (H2-2) --
        (B2) --
        (B3) --
        (H3-2) --
        (H3-1) --
        (A3) --
        (A4) --
        (H4-1) --
        (H4-2) --
        (B4) --
        (A1);

      \foreach \v in {A1,A2,H2-1,H2-2,B2,B3,H3-2,H3-1,A3,A4,H4-1,H4-2,B4}
        \node[vertex, draw=red] at (\v) {};
    \end{tikzpicture}
  \end{subfigure}
  \caption{The graph construction $G_{p,\ell}$ by C. T. Zamfirescu~\cite{article:zamfirescu} for $p=4$ and $\ell=3$, alongside the same graph but with one longest cycle marked in red}
  \label{fig:tube-graph-even}
\end{figure}

\subsection{Studying the Gadget}
\label{sec:gadget}

The construction by C. T. Zamfirescu starts off from two identical cycles with respective vertices connected. This graph is then modified, resulting in graphs with the desired properties. Studying this base graph, which we call a gadget, and seeing what properties it has and how modifying it results in the desired graphs could lead to new insights as to how to modify it differently to find new results for RQ\ref{rq2}.

In this thesis, we look deeper into the types of cycles that can be created from this gadget, and how inserting vertices on their edges changes the length of these cycles. It is this approach that has led to new negative results to the question of RQ\ref{rq2}.

\subsection{Almost-solution to RQ1}

\label{sec:modified-petersen}

While a solution for RQ\ref{rq1} is unknown, we do have an upper bound for the smallest amount of vertices in the intersection of all 11-cycle. In \cite{article:zamfirescu}, it is shown that a graph of circumference 11 can be constructed in which all but one vertex avoid all longest 11-cycles. This graph exploits the properties of the Petersen's graph, which is a hypohamiltonian graph of order 10 and thus circumference 9, and extends all three edges incident on one of its vertices $v$ with one extra vertex each, say $x_1$, $x_2$, and $x_3$, as seen in Figure~\ref{fig:petersen-divided}. This extends all cycles through this vertex $v$ by either 1 or 2. One can prove that as a result, this new graph of order 13 has circumference 11, with only one vertex meeting every 11-cycle~\cite{article:zamfirescu}. Note that this graph has a maximal degree of 3.

\begin{figure}
  \centering
  \begin{tikzpicture}[scale=1, every node/.style={circle,draw,fill=white,inner sep=2pt}
  ]

  % Outer pentagon
  \foreach \i/\a in {
    1/90,
    2/18,
    3/-54,
    4/-126,
    5/162
  }{
    \node (O\i) at (\a:2) {};
  }

  % Inner star vertices
  \foreach \i/\a in {
    1/90,
    2/18,
    3/-54,
    4/-126,
    5/162
  }{
    \node (I\i) at (\a:0.9) {};
  }

  % Subdivision vertices on edges adjacent to O1
  \node[draw,circle,fill=green!80] (S12) at ($(O1)!0.5!(O2)$) {};
  \node[draw,circle,fill=green!80] (S15) at ($(O1)!0.5!(O5)$) {};
  \node[draw,circle,fill=green!80] (S11) at ($(O1)!0.5!(I1)$) {};

  % Outer cycle
  \draw (O2)--(O3)--(O4)--(O5);
  \draw (O1)--(S12)--(O2);
  \draw (O1)--(S15)--(O5);
  \draw (O1)--(S11)--(I1);

  % Spokes
  \foreach \i in {2,...,5}{
    \draw (O\i)--(I\i);
  }

  % Inner star
  \draw (I1)--(I3)--(I5)--(I2)--(I4)--(I1);

  \end{tikzpicture}
  \caption{the Petersen graph, with three edges incident on one vertex extended by one extra vertex each}
  \label{fig:petersen-divided}
\end{figure}

\section{Conclusion}
\todo{Conclusion}
The final section of the chapter gives an overview of the important results
of this chapter. This implies that the introductory chapter and the
concluding chapter don't need a conclusion.

\begin{figure}
  \centering
  \label{fig:pipeline-1}
  \begin{tikzpicture}[node distance=4cm, auto]
  \node[draw, rectangle] (gen) {GenerateUHG};
  \node[draw, rectangle, right of=gen] (filter) {pickg};
  \node[draw, rectangle, right of=filter] (analysis) {van\_aardt\_intersect};

  \draw[->] (gen) -- node {graph6} (filter);
  \draw[->] (filter) -- node {graph6} (analysis);
  \draw[->] (analysis) -- node {CSV} ++(3,0);
  \end{tikzpicture}
  \caption{The pipeline for RQ\ref{rq1}}
\end{figure}

\begin{figure}
  \centering
  \label{fig:pipeline-2}
  \begin{tikzpicture}[node distance=4cm, auto]
  \node[draw, rectangle] (gen) {plantri};
  \node[draw, rectangle, right of=gen] (analysis) {k\_val\_check};

  \draw[->] (gen) -- node {graph6} (analysis);
  \draw[->] (analysis) -- node {CSV} ++(3,0);
  \end{tikzpicture}
  \caption{The pipeline for RQ\ref{rq2}}
\end{figure}
